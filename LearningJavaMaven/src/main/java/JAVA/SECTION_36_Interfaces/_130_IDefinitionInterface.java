
package JAVA.SECTION_36_Interfaces;

/*
    --an interface seems to an abstract class, because the methods of an interface has not an implementation, so also are abstract methods
    --all methods that are defined in a class are abastract+
    --if we add attribute to an interface, these will be constants, it means that are attributes of type final, public and static
    --then the diefference between an abstract class and an interface is 
    --if the relation between classes is by FEATTURES we use abstract class
    --if the relation between classes is by BEHAVIOR, then we use interfaces

    --while wtih classes we abstract common features, with interfaces we abstract common behavior
*/
public class _130_IDefinitionInterface {
    
}
